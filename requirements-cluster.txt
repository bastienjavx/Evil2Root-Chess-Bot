# Dépendances du COORDINATEUR de cluster (sanchess.cluster.server) — déployé sur Railway.
# Volontairement MINIMAL et SANS torch/numpy : le coordinateur ne fait que router des
# octets et valider des shards de samples avec python-chess. Image légère, démarrage rapide.
fastapi>=0.137.1
uvicorn[standard]>=0.49.0
python-multipart>=0.0.32   # parsing des uploads multipart (/upload, /model/publish)
python-chess>=1.999       # validation des FEN / coups des shards reçus
