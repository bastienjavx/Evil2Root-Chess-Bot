# Coeur ML — installe PyTorch avec CUDA séparément si besoin :
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.2
numpy>=1.26

# Échecs
python-chess>=1.999

# Données Lichess
zstandard>=0.22      # décompression des dumps .pgn.zst
requests>=2.31       # API Lichess (stream temps réel)

# Config / utilitaires
pyyaml>=6.0.3
tqdm>=4.68.2

# Interface web (API REST + WebSocket + frontend) — sanchess.web
fastapi>=0.137.0
uvicorn[standard]>=0.27
